*** Begin Patch
*** Update File:services/models/multimodal/train_multimodal.py
@@
-import torch
-import torch.nn as nn
-import torch.optim as optim
-from torchvision import transforms
-from PIL import Image
-
-try:
-    from services.fl.fl_controller_dp_wrapper import check_and_start_round, report_round
-except Exception:
-    # If wrapper not installed, provide no-op fallbacks for local testing
-    def check_and_start_round(*args, **kwargs):
-        return "local-run-id"
-    def report_round(*args, **kwargs):
-        return {"ok": True}
+import os
+import json
+import argparse
+import time
+from PIL import Image
+
+# Optional dependency: PyTorch. If not present (CI or lightweight runners), fall back to a numpy-based mock training loop.
+TORCH_AVAILABLE = True
+try:
+    import torch
+    import torch.nn as nn
+    import torch.optim as optim
+    from torchvision import transforms
+except Exception:
+    TORCH_AVAILABLE = False
+    # Provide a minimal transforms shim used in the numpy fallback
+    class _DummyTransforms:
+        @staticmethod
+        def Resize(size):
+            return lambda img: img.resize(size)
+        @staticmethod
+        def ToTensor():
+            import numpy as _np
+            return lambda img: _np.array(img).astype("float32").transpose(2,0,1) / 255.0
+    transforms = _DummyTransforms()
+
+try:
+    from services.fl.fl_controller_dp_wrapper import check_and_start_round, report_round
+except Exception:
+    # If wrapper not installed, provide no-op fallbacks for local testing
+    def check_and_start_round(*args, **kwargs):
+        return "local-run-id"
+    def report_round(*args, **kwargs):
+        return {"ok": True}
@@
-class TinyMultimodal(nn.Module):
-    def __init__(self):
-        super().__init__()
-        self.img_net = nn.Sequential(nn.Flatten(), nn.Linear(64*64*3, 64), nn.ReLU())
-        self.txt_net = nn.Embedding(1000, 16)
-        self.classifier = nn.Sequential(nn.Linear(64+16, 32), nn.ReLU(), nn.Linear(32,2))
-    def forward(self, img, txt_idx):
-        i = self.img_net(img)
-        t = self.txt_net(txt_idx).mean(dim=1)
-        x = torch.cat([i,t], dim=1)
-        return self.classifier(x)
+if TORCH_AVAILABLE:
+    class TinyMultimodal(nn.Module):
+        def __init__(self):
+            super().__init__()
+            self.img_net = nn.Sequential(nn.Flatten(), nn.Linear(64*64*3, 64), nn.ReLU())
+            self.txt_net = nn.Embedding(1000, 16)
+            self.classifier = nn.Sequential(nn.Linear(64+16, 32), nn.ReLU(), nn.Linear(32,2))
+        def forward(self, img, txt_idx):
+            i = self.img_net(img)
+            t = self.txt_net(txt_idx).mean(dim=1)
+            x = torch.cat([i,t], dim=1)
+            return self.classifier(x)
+else:
+    # Numpy fallback model: simple linear model with random weights; used for CI/lightweight runs where PyTorch isn't available.
+    import numpy as np
+    class TinyMultimodal:
+        def __init__(self):
+            # shapes: image flattened -> 64, text embedding -> 16
+            self.w_img = np.random.randn(64*64*3, 64).astype("float32") * 0.01
+            self.w_txt = np.random.randn(1000, 16).astype("float32") * 0.01
+            self.w_clf = np.random.randn(64+16, 2).astype("float32") * 0.01
+        def __call__(self, img_batch, txt_idx_batch):
+            # img_batch: numpy (N, 64*64*3), txt_idx_batch: (N,)
+            img_proj = img_batch.dot(self.w_img)  # (N,64)
+            # simple embedding lookup average (mock)
+            txt_emb = np.stack([self.w_txt[idx % self.w_txt.shape[0]] for idx in txt_idx_batch], axis=0)
+            x = np.concatenate([img_proj, txt_emb], axis=1)
+            logits = x.dot(self.w_clf)
+            return logits
@@
-def text_to_index(s):
-    return sum(ord(c) for c in s) % 1000
+def text_to_index(s):
+    return sum(ord(c) for c in s) % 1000
@@
-def load_dataset(data_dir):
-    with open(os.path.join(data_dir, "manifest.json")) as fh:
-        manifest = json.load(fh)
-    X_img, X_txt, y = [], [], []
-    tf = transforms.Compose([transforms.Resize((64,64)), transforms.ToTensor()])
-    for rec in manifest:
-        img = Image.open(rec["image"]).convert("RGB")
-        X_img.append(tf(img).view(-1))
-        X_txt.append(text_to_index(rec["text"]))
-        y.append(rec["label"])
-    return torch.stack(X_img), torch.tensor(X_txt), torch.tensor(y)
+def load_dataset(data_dir):
+    with open(os.path.join(data_dir, "manifest.json")) as fh:
+        manifest = json.load(fh)
+    X_img, X_txt, y = [], [], []
+    if TORCH_AVAILABLE:
+        tf = transforms.Compose([transforms.Resize((64,64)), transforms.ToTensor()])
+        for rec in manifest:
+            img = Image.open(rec["image"]).convert("RGB")
+            X_img.append(tf(img).view(-1))
+            X_txt.append(text_to_index(rec["text"]))
+            y.append(rec["label"])
+        return torch.stack(X_img), torch.tensor(X_txt), torch.tensor(y)
+    else:
+        import numpy as np
+        for rec in manifest:
+            arr = np.load(rec["image"])["image"] if rec["image"].endswith(".npz") else np.array(Image.open(rec["image"]).resize((64,64))).astype("float32")/255.0
+            X_img.append(arr.reshape(-1))
+            X_txt.append(text_to_index(rec["text"]))
+            y.append(rec["label"])
+        return np.stack(X_img), np.array(X_txt), np.array(y)
@@
-def train(data_dir, epochs=1):
-    # DP check before training round
-    run_id = check_and_start_round(project="multimodal-demo", noise=1.0, sample_rate=0.01, steps=epochs)
-    X_img, X_txt, y = load_dataset(data_dir)
-    model = TinyMultimodal()
-    optim_ = optim.Adam(model.parameters(), lr=1e-3)
-    loss_fn = nn.CrossEntropyLoss()
-    for e in range(epochs):
-        model.train()
-        optim_.zero_grad()
-        logits = model(X_img, X_txt.unsqueeze(1))
-        loss = loss_fn(logits, y)
-        loss.backward()
-        optim_.step()
-        print(f"epoch {e} loss {loss.item()}")
-    # compute a toy epsilon to report
-    epsilon = 0.5
-    report = report_round(project="multimodal-demo", run_id=run_id, epsilon=epsilon)
-    print("DP report:", report)
+def train(data_dir, epochs=1):
+    # DP check before training round
+    run_id = check_and_start_round(project="multimodal-demo", noise=1.0, sample_rate=0.01, steps=epochs)
+    X_img, X_txt, y = load_dataset(data_dir)
+    if TORCH_AVAILABLE:
+        model = TinyMultimodal()
+        optim_ = optim.Adam(model.parameters(), lr=1e-3)
+        loss_fn = nn.CrossEntropyLoss()
+        for e in range(epochs):
+            model.train()
+            optim_.zero_grad()
+            logits = model(X_img, X_txt.unsqueeze(1))
+            loss = loss_fn(logits, y)
+            loss.backward()
+            optim_.step()
+            print(f"epoch {e} loss {loss.item()}")
+    else:
+        # Numpy fallback: simple mock "training" loop that simulates work and prints loss-like metric.
+        import numpy as np
+        model = TinyMultimodal()
+        for e in range(epochs):
+            logits = model(X_img, X_txt)
+            # simple softmax + cross-entropy-ish scalar for logging
+            exps = np.exp(logits - logits.max(axis=1, keepdims=True))
+            probs = exps / exps.sum(axis=1, keepdims=True)
+            # one-hot labels
+            labels = np.eye(probs.shape[1])[y]
+            loss = - (labels * np.log(probs + 1e-8)).sum() / probs.shape[0]
+            print(f"[mock] epoch {e} loss {float(loss):.4f}")
+    # compute a toy epsilon to report
+    epsilon = 0.5
+    report = report_round(project="multimodal-demo", run_id=run_id, epsilon=epsilon)
+    print("DP report:", report)
*** End Patch
