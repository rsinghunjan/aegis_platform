*** Begin Patch
*** Add File:ops/llm_support/cache/requirements.txt
cachetools>=5.3.0
redis>=4.5.0
prometheus_client>=0.16.0
fastapi>=0.95.0
uvicorn[standard]>=0.20.0
httpx>=0.24.0
python-dotenv>=1.0.0
*** End File
*** Add File:ops/llm_support/cache/cache.py
```python name=ops/llm_support/cache/cache.py
"""
Local in-memory TTL LRU cache for prompt/response caching.

Usage:
    from aegis_cache import LocalCache
    cache = LocalCache(maxsize=1024, ttl=3600)
    cache.set(key, value)
    v = cache.get(key)
"""
from cachetools import TTLCache
import threading
import time
import logging

LOG = logging.getLogger("aegis.cache.local")


class LocalCache:
    def __init__(self, maxsize: int = 1024, ttl: int = 3600):
        self._cache = TTLCache(maxsize=maxsize, ttl=ttl)
        self._lock = threading.RLock()

    def get(self, key: str):
        with self._lock:
            try:
                return self._cache[key]
            except KeyError:
                return None

    def set(self, key: str, value, ttl: int = None):
        with self._lock:
            if ttl is None:
                # TTLCache honors its global ttl
                self._cache[key] = value
            else:
                # Emulate per-entry TTL by wrapping value with metadata
                expire_at = int(time.time()) + ttl
                self._cache[key] = {"_v": value, "_e": expire_at}

    def pop(self, key: str):
        with self._lock:
            return self._cache.pop(key, None)

    def clear(self):
        with self._lock:
            self._cache.clear()

    def keys(self):
        with self._lock:
            return list(self._cache.keys())

    def __len__(self):
        with self._lock:
            return len(self._cache)
