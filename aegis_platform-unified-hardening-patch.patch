diff --git a/.github/workflows/e2e-deploy-and-verify.yml b/.github/workflows/e2e-deploy-and-verify.yml
index 773a4aef366b..f0e858db8cc1 100644
--- a/.github/workflows/e2e-deploy-and-verify.yml
+++ b/.github/workflows/e2e-deploy-and-verify.yml
@@ -156,20 +156,20 @@ jobs:
 
       - name: Wait for pods to be ready
         run: |
-          kubectl -n aegis rollout status deployment/aegis-orchestrator --timeout=300s || true
+          kubectl -n aegis rollout status deployment/aegis-orchestrator --timeout=300s
           kubectl -n aegis get pods -o wide
 
       - name: Run verifier smoke (uses configured AWS creds via OIDC)
         env:
           OBJECT_STORE_TYPE: s3
         run: |
-          python3 scripts/verify_model_signatures.py || true
+          python3 scripts/verify_model_signatures.py
 
       - name: Run canary simulation (simple)
         run: |
           pip install httpx
-          python3 scripts/simulate_canary_load.py --url "http://aegis-staging.example.com/infer" --qps 10 --duration 60 --error-rate 0.02 || true
+          python3 scripts/simulate_canary_load.py --url "http://aegis-staging.example.com/infer" --qps 10 --duration 60 --error-rate 0.02
 
       - name: Snapshot DB backup & restore test (optional)
         run: |
-          ./scripts/backup/backup_db.py || true
+          ./scripts/backup/backup_db.py
           echo "Manual restore test recommended; see docs/production_ready_runbook.md"
diff --git a/.github/workflows/mlflow-training.yml b/.github/workflows/mlflow-training.yml
index f208f5a7264e..f833bcb814a9 100644
--- a/.github/workflows/mlflow-training.yml
+++ b/.github/workflows/mlflow-training.yml
@@ -68,9 +68,20 @@ jobs:
           sudo apt-get update
           sudo apt-get install -y docker-compose-plugin docker.io
           docker compose -f docker/docker-compose.mlflow.yml up -d --build
-          # wait for mlflow to accept connections
+          # wait for mlflow to accept connections (bounded)
           echo "Waiting for MLflow to be ready..."
-          until curl -sSf http://localhost:5000/health || true; do sleep 2; done
-          echo "MLflow appears reachable"
+          for i in $(seq 1 60); do
+            if curl -sSf http://localhost:5000/health >/dev/null; then
+              echo "MLflow appears reachable"
+              exit 0
+            fi
+            sleep 2
+          done
+          echo "ERROR: MLflow did not become healthy within timeout" >&2
+          docker compose -f docker/docker-compose.mlflow.yml ps || true
+          docker compose -f docker/docker-compose.mlflow.yml logs --no-color --tail=200 || true
+          exit 1
diff --git a/.github/workflows/promote_model_if_valid.yml b/.github/workflows/promote_model_if_valid.yml
index d675aba95a73..1c6eaa216222 100644
--- a/.github/workflows/promote_model_if_valid.yml
+++ b/.github/workflows/promote_model_if_valid.yml
@@ -96,10 +96,10 @@ jobs:
             --mode "${{ inputs.mode }}" \
             --metric "${{ inputs.metric }}" \
             --baseline "${{ inputs.baseline }}" \
             --tolerance "${{ inputs.tolerance }}" \
-            --emit-json validation_result.json || true
+            --emit-json validation_result.json
 
-          cat validation_result.json || echo '{}' > validation_result.json
+          cat validation_result.json
           STATUS=$(jq -r '.status // "error"' validation_result.json)
           echo "Validation status: $STATUS"
           # expose outputs for later steps / job output
diff --git a/.github/workflows/security-ci.yml b/.github/workflows/security-ci.yml
index 188561260966..22b5d5da0b9a 100644
--- a/.github/workflows/security-ci.yml
+++ b/.github/workflows/security-ci.yml
@@ -31,7 +31,11 @@ jobs:
 
       - name: Install project deps (for static checks)
         run: |
-          if [ -f aegis_multimodal_ai_system/requirements.txt ]; then pip install -r aegis_multimodal_ai_system/requirements.txt || true; fi
+          if [ -f aegis_multimodal_ai_system/requirements.txt ]; then
+            pip install -r aegis_multimodal_ai_system/requirements.txt
+          else
+            echo "No aegis_multimodal_ai_system/requirements.txt found; skipping project deps install"
+          fi
diff --git a/scripts/check_filenames.py b/scripts/check_filenames.py
index 0e3b16594b..3b59515b2d 100755
--- a/scripts/check_filenames.py
+++ b/scripts/check_filenames.py
@@ -1,33 +1,45 @@
 #!/usr/bin/env python
 """
 Repo hygiene filename checks.
 
 - Ensures mlflow compose filename consistency: accepts either docker/docker-compose-mlflow.yml or docker/docker-compose.mlflow.yml but flags if none or both are present.
+- Warns if patch/diff bundles are dropped at repo root (prefer docs/patches/ or patches/).
 - Optional additional checks can be added to enforce naming conventions.
 
 Usage:
   python scripts/check_filenames.py
 Exit non-zero on failure.
 """
 from pathlib import Path
 import sys
 
 def main():
     repo_root = Path(".")
+
+    # Root-level patch/diff files create ambiguity and bloat. Keep them organized under docs/patches/ or patches/.
+    root_patch_like = sorted([p for p in repo_root.iterdir() if p.is_file() and p.suffix in {".patch", ".diff"}])
+    if root_patch_like:
+        print("WARNING: Root-level *.patch/*.diff files found; please move them under docs/patches/ or patches/:")
+        for p in root_patch_like:
+            print(f"  - {p.name}")
+
     f1 = repo_root / "docker" / "docker-compose-mlflow.yml"
     f2 = repo_root / "docker" / "docker-compose.mlflow.yml"
     has1 = f1.exists()
     has2 = f2.exists()
     if not has1 and not has2:
         print("ERROR: No MLflow docker compose file found. Expected one of:")
         print("  docker/docker-compose-mlflow.yml")
         print("  docker/docker-compose.mlflow.yml")
         return 2
     if has1 and has2:
         print("WARNING: Both compose filenames present. Please keep a single canonical name to avoid confusion.")
     else:
         print("OK: Found compose file:", f1 if has1 else f2)
     return 0
 
 if __name__ == "__main__":
     sys.exit(main())
