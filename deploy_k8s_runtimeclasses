*** Begin Patch
*** Add File:deploy/k8s/runtimeclasses.yaml
+apiVersion: node.k8s.io/v1
+kind: RuntimeClass
+metadata:
+  name: runsc
+handler: runsc
+
+---
+apiVersion: node.k8s.io/v1
+kind: RuntimeClass
+metadata:
+  name: kata
+handler: kata
+
+# Notes:
+# - Ensure the container runtime on your nodes (containerd/cri-o) has runsc and/or kata installed and configured.
+# - Use `pod.spec.runtimeClassName: "runsc"` or `"kata"` for high-risk workloads that require stronger isolation.
+# - Combine this with seccomp profiles, non-root containers, network policies, and image allowlists (cosign-signed images)
+#   for a layered defense to mitigate container escape risk for long-running inference jobs.
*** End Patch
