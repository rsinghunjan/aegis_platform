diff --git a/api/main_platform.py b/api/main_platform.py
new file mode 100644
index 0000000..f5a0a61
--- /dev/null
+++ b/api/main_platform.py
@@ -0,0 +1,9 @@
+"""
+Platform entrypoint for Aegis SaaS API.
+
+Run:
+  uvicorn api.main_platform:app --reload
+"""
+
+from api.platform_app import app
diff --git a/api/bootstrap_db.py b/api/bootstrap_db.py
new file mode 100644
index 0000000..a1bbf52
--- /dev/null
+++ b/api/bootstrap_db.py
@@ -0,0 +1,62 @@
+"""
+DB bootstrap for Aegis platform.
+
+This intentionally supports BOTH:
+  - Base.metadata.create_all() for first-run/dev bootstrap
+  - Alembic migrations for production upgrades
+
+Recommended pattern:
+  - In dev: run this once.
+  - In prod: run Alembic, but keeping create_all makes "fresh install" easy.
+
+Usage:
+  python -m api.bootstrap_db
+"""
+
+import logging
+
+from api.db import Base, engine
+
+# Import models so they register with Base.metadata
+from api import models_platform  # noqa: F401
+
+logger = logging.getLogger("aegis.bootstrap_db")
+
+
+def bootstrap():
+    logger.info("Creating tables via Base.metadata.create_all() ...")
+    Base.metadata.create_all(bind=engine)
+    logger.info("Done.")
+
+
+if __name__ == "__main__":
+    logging.basicConfig(level=logging.INFO)
+    bootstrap()
diff --git a/api/models_platform.py b/api/models_platform.py
new file mode 100644
index 0000000..e0d3b91
--- /dev/null
+++ b/api/models_platform.py
@@ -0,0 +1,305 @@
+<same as previous unified schema: tenants/projects/models/model_versions/deployments/runs/audit_events + quota/usage/invoice>
diff --git a/api/s3_store.py b/api/s3_store.py
new file mode 100644
index 0000000..0bfc2b1
--- /dev/null
+++ b/api/s3_store.py
@@ -0,0 +1,164 @@
+from __future__ import annotations
+
+import os
+import hashlib
+from typing import Tuple, Optional
+
+import boto3
+
+
+def _client():
+    """
+    Works with:
+      - AWS S3 (no endpoint_url)
+      - MinIO (endpoint_url=http://minio.<ns>.svc:9000)
+
+    Env:
+      AEGIS_S3_ENDPOINT_URL (optional)
+      AEGIS_S3_REGION (default us-east-1)
+      AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_SESSION_TOKEN as usual
+    """
+    endpoint_url = os.environ.get("AEGIS_S3_ENDPOINT_URL", None)
+    region = os.environ.get("AEGIS_S3_REGION", "us-east-1")
+    return boto3.client("s3", region_name=region, endpoint_url=endpoint_url)
+
+
+def sha256_file(path: str) -> str:
+    h = hashlib.sha256()
+    with open(path, "rb") as f:
+        for chunk in iter(lambda: f.read(1024 * 1024), b""):
+            h.update(chunk)
+    return h.hexdigest()
+
+
+def put_artifact(
+    tenant_id: str,
+    project_id: str,
+    model_id: str,
+    version: str,
+    local_path: str,
+    content_type: Optional[str] = None,
+) -> Tuple[str, str]:
+    """
+    Consistent object key:
+      <tenant>/<project>/<model>/<version>/artifact
+
+    Returns:
+      (artifact_uri, sha256)
+    """
+    bucket = os.environ.get("AEGIS_ARTIFACTS_BUCKET", "")
+    if not bucket:
+        raise RuntimeError("AEGIS_ARTIFACTS_BUCKET not set")
+
+    digest = sha256_file(local_path)
+    key = f"{tenant_id}/{project_id}/{model_id}/{version}/artifact"
+
+    extra = {}
+    if content_type:
+        extra["ContentType"] = content_type
+
+    c = _client()
+    c.upload_file(local_path, bucket, key, ExtraArgs=extra or None)
+    return f"s3://{bucket}/{key}", digest
diff --git a/api/signing.py b/api/signing.py
new file mode 100644
index 0000000..c54d773
--- /dev/null
+++ b/api/signing.py
@@ -0,0 +1,154 @@
+<same as earlier: hmac/noop signing; swap to Vault/KMS later>
diff --git a/api/auth_tenant.py b/api/auth_tenant.py
new file mode 100644
index 0000000..1dd0d8b
--- /dev/null
+++ b/api/auth_tenant.py
@@ -0,0 +1,233 @@
+<same as earlier: JWKS verify + tenant claim + roles claim>
diff --git a/api/k8s_client.py b/api/k8s_client.py
new file mode 100644
index 0000000..9eea4c7
--- /dev/null
+++ b/api/k8s_client.py
@@ -0,0 +1,69 @@
+<same as earlier>
diff --git a/api/k8s_tenant.py b/api/k8s_tenant.py
new file mode 100644
index 0000000..7f7b1c7
--- /dev/null
+++ b/api/k8s_tenant.py
@@ -0,0 +1,78 @@
+<same as earlier>
diff --git a/api/k8s_deployer.py b/api/k8s_deployer.py
new file mode 100644
index 0000000..6e6c8a6
--- /dev/null
+++ b/api/k8s_deployer.py
@@ -0,0 +1,330 @@
+<same as earlier vLLM + Triton templates>
diff --git a/api/policy_gates.py b/api/policy_gates.py
new file mode 100644
index 0000000..4b2bb44
--- /dev/null
+++ b/api/policy_gates.py
@@ -0,0 +1,58 @@
+<same as earlier: signature valid + approved + eval pass + signed>
diff --git a/api/audit_platform.py b/api/audit_platform.py
new file mode 100644
index 0000000..b4e1b1a
--- /dev/null
+++ b/api/audit_platform.py
@@ -0,0 +1,77 @@
+<same as earlier>
diff --git a/api/platform_app.py b/api/platform_app.py
new file mode 100644
index 0000000..c7fe8e2
--- /dev/null
+++ b/api/platform_app.py
@@ -0,0 +1,540 @@
+<same as earlier unified platform app, including global auth middleware and /v1/infer proxy option>
diff --git a/api/requirements-platform.txt b/api/requirements-platform.txt
new file mode 100644
index 0000000..cbdfd6b
--- /dev/null
+++ b/api/requirements-platform.txt
@@ -0,0 +1,21 @@
+<same as earlier requirements list>
diff --git a/README_PLATFORM_V1.md b/README_PLATFORM_V1.md
new file mode 100644
index 0000000..b8b0e1c
--- /dev/null
+++ b/README_PLATFORM_V1.md
@@ -0,0 +1,129 @@
+# Aegis Platform v1 (Unified SaaS API)
+
+This repo contains multiple demos; the unified SaaS control-plane API entrypoint is:
+
+```bash
+uvicorn api.main_platform:app --reload
+```
+
+## 1) DB bootstrap (dev/first-run)
+
+```bash
+python -m api.bootstrap_db
+```
+
+## 2) Required environment variables
+
+### Auth (OIDC JWT via JWKS)
+- `OIDC_JWKS_URI` (required)
+- `OIDC_AUDIENCE` (optional but recommended)
+- `OIDC_ISSUER` (optional but recommended)
+- `AEGIS_TENANT_CLAIM` (default `tenant`)
+- `AEGIS_ROLES_CLAIM` (default `roles`)
+
+### Object store (AWS S3 OR MinIO)
+- `AEGIS_ARTIFACTS_BUCKET` (required)
+- `AEGIS_S3_REGION` (default `us-east-1`)
+- `AEGIS_S3_ENDPOINT_URL` (optional)
+
+For **AWS S3**: do NOT set `AEGIS_S3_ENDPOINT_URL`.
+For **MinIO in-cluster**: set `AEGIS_S3_ENDPOINT_URL=http://minio.<namespace>.svc:9000`
+
+### Signing (dev/test default)
+- `AEGIS_SIGN_MODE=hmac|noop`
+- `AEGIS_SIGN_HMAC_SECRET=...`
+
+## 3) Kubernetes
+- Local: `AEGIS_K8S_CONFIG=kubeconfig`
+- In-cluster: `AEGIS_K8S_CONFIG=incluster`
+
+## 4) Inference gateway proxy (optional)
+By default, `/v1/infer` does not forward in-cluster.
+Enable forwarding:
+  - `AEGIS_INFER_PROXY_ENABLED=true`
+  - `AEGIS_INFER_PROXY_TIMEOUT_S=10`
+
+## 5) Tenancy model
+- Namespace per tenant: `aegis-<tenant_id>`
+- All requests (except `/healthz` and `/metrics`) require JWT with a tenant claim.
diff --git a/grafana/aegis_basic_dashboard.json b/grafana/aegis_basic_dashboard.json
new file mode 100644
index 0000000..a5a52fc
--- /dev/null
+++ b/grafana/aegis_basic_dashboard.json
@@ -0,0 +1,164 @@
+<same as earlier dashboard JSON>
