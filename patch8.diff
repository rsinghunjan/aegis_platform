diff --git a/api/golden_k8s.py b/api/golden_k8s.py
index 8a0f2b3..dff5c8b 100644
--- a/api/golden_k8s.py
+++ b/api/golden_k8s.py
@@ -1,12 +1,11 @@
 from __future__ import annotations
 
 import os
 import re
 from typing import Dict, Optional, Tuple
 
 from kubernetes import client, config
 from kubernetes.client.rest import ApiException
 
 
 def _load_k8s():
@@ -32,47 +31,6 @@ def _apps() -> client.AppsV1Api:
     _load_k8s()
     return client.AppsV1Api()
 
 
-def _maybe_create_secret(namespace: str, name: str, data: Dict[str, str], labels: Optional[Dict[str, str]] = None):
-    """
-    Best-effort: create a secret if it doesn't exist.
-    NOTE: This stores sensitive data in a Kubernetes Secret; use External Secrets / Vault Agent in production.
-    """
-    core = _core()
-    try:
-        core.read_namespaced_secret(name=name, namespace=namespace)
-        return
-    except ApiException as e:
-        if e.status != 404:
-            raise
-    body = client.V1Secret(
-        metadata=client.V1ObjectMeta(name=name, namespace=namespace, labels=labels or {}),
-        type="Opaque",
-        string_data=data,
-    )
-    core.create_namespaced_secret(namespace=namespace, body=body)
-
-
 def ensure_tenant_namespace(tenant_id: str) -> str:
     ns = f"aegis-{tenant_id}"
     core = _core()
     try:
@@ -90,124 +48,6 @@ def _sanitize_name(s: str) -> str:
     return s[:50] if len(s) > 50 else s
 
 
-def _storage_env_and_secrets(namespace: str) -> Tuple[list[client.V1EnvVar], list[client.V1EnvFromSource]]:
-    """
-    Provide S3/MinIO credentials to initContainers.
-
-    Supported modes:
-      1) AWS in-cluster with IRSA/instance profile: no explicit creds required.
-      2) MinIO / static creds: set AEGIS_MINIO_ACCESS_KEY + AEGIS_MINIO_SECRET_KEY
-         -> secret aegis-s3-creds will be created in the tenant namespace.
-
-    This keeps the patch self-contained. For production, use:
-      - External Secrets Operator, or
-      - Vault Agent injector + templates, or
-      - AWS IRSA only.
-    """
-    env: list[client.V1EnvVar] = [
-        client.V1EnvVar(name="AEGIS_S3_ENDPOINT_URL", value=os.environ.get("AEGIS_S3_ENDPOINT_URL", "")),
-        client.V1EnvVar(name="AEGIS_S3_REGION", value=os.environ.get("AEGIS_S3_REGION", "us-east-1")),
-    ]
-    env_from: list[client.V1EnvFromSource] = []
-
-    ak = os.environ.get("AEGIS_MINIO_ACCESS_KEY", "")
-    sk = os.environ.get("AEGIS_MINIO_SECRET_KEY", "")
-    if ak and sk:
-        _maybe_create_secret(
-            namespace=namespace,
-            name="aegis-s3-creds",
-            data={
-                "AWS_ACCESS_KEY_ID": ak,
-                "AWS_SECRET_ACCESS_KEY": sk,
-            },
-            labels={"aegis.io/managed-by": "aegis-golden-api"},
-        )
-        env_from.append(client.V1EnvFromSource(secret_ref=client.V1SecretEnvSource(name="aegis-s3-creds")))
-    # else: rely on AWS ambient credentials (IRSA / node role / local env)
-    return env, env_from
-
-
-def _vault_env_and_secrets(namespace: str) -> Tuple[list[client.V1EnvVar], list[client.V1EnvFromSource]]:
-    """
-    Provide Vault auth to initContainers.
-
-    Recommended production patterns:
-      - Vault Agent Injector (sidecar/init) using Kubernetes auth (no tokens in env)
-      - OR External Secrets synced from Vault into K8s Secret
-
-    Self-contained patch option:
-      - if AEGIS_VAULT_TOKEN is set, create secret aegis-vault-token in tenant namespace.
-        (Not recommended long-term.)
-    """
-    env: list[client.V1EnvVar] = [
-        client.V1EnvVar(name="VAULT_ADDR", value=os.environ.get("VAULT_ADDR", "")),
-        client.V1EnvVar(name="AEGIS_VAULT_TRANSIT_MOUNT", value=os.environ.get("AEGIS_VAULT_TRANSIT_MOUNT", "transit")),
-        client.V1EnvVar(name="AEGIS_VAULT_TRANSIT_KEY", value=os.environ.get("AEGIS_VAULT_TRANSIT_KEY", "aegis-model-sign")),
-    ]
-    env_from: list[client.V1EnvFromSource] = []
-
-    token = os.environ.get("AEGIS_VAULT_TOKEN", "") or os.environ.get("VAULT_TOKEN", "")
-    if token:
-        _maybe_create_secret(
-            namespace=namespace,
-            name="aegis-vault-token",
-            data={"VAULT_TOKEN": token},
-            labels={"aegis.io/managed-by": "aegis-golden-api"},
-        )
-        env_from.append(client.V1EnvFromSource(secret_ref=client.V1SecretEnvSource(name="aegis-vault-token")))
-    return env, env_from
-
-
 def _artifact_init_container(
     name: str,
     artifact_uri: str,
     artifact_sha256: str,
     signature: str,
@@ -217,21 +57,10 @@ def _artifact_init_container(
     bucket, key = _parse_s3_uri(artifact_uri)
     image = os.environ.get("AEGIS_ARTIFACT_INIT_IMAGE", "python:3.11-slim")
     # Uses boto3 + requests in a tiny inline script to keep this patch self-contained.
     # In production, replace with a hardened image.
     cmd = r"""
 set -euo pipefail
 python -V
-python - <<'PIP'
-import sys,subprocess
-subprocess.check_call([sys.executable,"-m","pip","install","--no-cache-dir","boto3==1.34.131","requests==2.32.3"])
-print("deps installed")
-PIP
 python - <<'PY'
 import os,sys,hashlib,base64,tarfile,shutil
 import boto3
 import requests
@@ -250,21 +79,49 @@ def _artifact_init_container(
 def verify_vault_transit(hex_sha, signature):
     vault_addr=os.environ.get("VAULT_ADDR","")
     vault_token=os.environ.get("VAULT_TOKEN","")
     mount=os.environ.get("AEGIS_VAULT_TRANSIT_MOUNT","transit")
     keyname=os.environ.get("AEGIS_VAULT_TRANSIT_KEY","aegis-model-sign")
-    if not (vault_addr and vault_token):
-        raise RuntimeError("VAULT_ADDR and VAULT_TOKEN required for vault_transit verification")
+    if not vault_addr:
+        raise RuntimeError("VAULT_ADDR required for vault_transit verification")
+    # Hardened: obtain token via Vault Agent injector file if present
+    if not vault_token:
+        token_file=os.environ.get("VAULT_TOKEN_FILE","/vault/secrets/token")
+        try:
+            vault_token=open(token_file,"r").read().strip()
+        except Exception:
+            vault_token=""
+    if not vault_token:
+        raise RuntimeError("Vault token missing. Use Vault Agent injector to provide /vault/secrets/token (or set VAULT_TOKEN).")
     payload_b64=base64.b64encode(hex_sha.encode()).decode()
     url=f"{vault_addr}/v1/{mount}/verify/{keyname}"
     r=requests.post(url, headers={"X-Vault-Token": vault_token}, json={"input": payload_b64, "signature": signature}, timeout=10)
     r.raise_for_status()
     return bool(r.json()["data"]["valid"])
@@ -272,9 +129,18 @@ def _artifact_init_container(
 print("Downloading s3://%s/%s" % (bucket,key))
 c=boto3.client("s3", region_name=region, endpoint_url=endpoint)
 c.download_file(bucket, key, artifact_path)
 
 actual=sha256_file(artifact_path)
 if actual != expected_sha:
     raise SystemExit(f"sha256 mismatch expected={expected_sha} actual={actual}")
 
 if sig_type != "noop":
     if not sig:
         raise SystemExit("missing signature")
     if sig_type == "hmac":
         if not verify_hmac(expected_sha, sig):
             raise SystemExit("HMAC signature invalid")
     elif sig_type == "vault_transit":
         if not verify_vault_transit(expected_sha, sig):
             raise SystemExit("Vault Transit signature invalid")
     else:
         raise SystemExit(f"unknown signature type: {sig_type}")
@@ -288,6 +154,15 @@ if fname.endswith(".tar.gz") or fname.endswith(".tgz"):
 else:
     # fallback: copy single file to /models/artifact
     shutil.copyfile(artifact_path, os.path.join(outdir, "artifact"))
 
 print("Artifact ready in", outdir)
 PY
 """
     return client.V1Container(
         name=name,
-        image=image,
+        image=image,
         command=["/bin/sh", "-lc", cmd],
         env=[
             client.V1EnvVar(name="AEGIS_S3_BUCKET", value=bucket),
             client.V1EnvVar(name="AEGIS_S3_KEY", value=key),
             client.V1EnvVar(name="AEGIS_ARTIFACT_FILENAME", value=os.environ.get("AEGIS_ARTIFACT_FILENAME", "artifact")),
             client.V1EnvVar(name="AEGIS_S3_ENDPOINT_URL", value=os.environ.get("AEGIS_S3_ENDPOINT_URL", "")),
             client.V1EnvVar(name="AEGIS_S3_REGION", value=os.environ.get("AEGIS_S3_REGION", "us-east-1")),
             client.V1EnvVar(name="AEGIS_ARTIFACT_SHA256", value=artifact_sha256),
             client.V1EnvVar(name="AEGIS_ARTIFACT_SIGNATURE", value=signature or ""),
             client.V1EnvVar(name="AEGIS_SIGNATURE_TYPE", value=signature_type or "hmac"),
             client.V1EnvVar(name="AEGIS_MODELS_DIR", value=mount_path),
             # Vault vars (only used if signature_type=vault_transit)
             client.V1EnvVar(name="VAULT_ADDR", value=os.environ.get("VAULT_ADDR", "")),
+            client.V1EnvVar(name="VAULT_TOKEN_FILE", value=os.environ.get("VAULT_TOKEN_FILE", "/vault/secrets/token")),
             client.V1EnvVar(name="AEGIS_VAULT_TRANSIT_MOUNT", value=os.environ.get("AEGIS_VAULT_TRANSIT_MOUNT", "transit")),
             client.V1EnvVar(name="AEGIS_VAULT_TRANSIT_KEY", value=os.environ.get("AEGIS_VAULT_TRANSIT_KEY", "aegis-model-sign")),
             client.V1EnvVar(name="AEGIS_SIGN_HMAC_SECRET", value=os.environ.get("AEGIS_SIGN_HMAC_SECRET", "")),
         ],
-        # NOTE: envFrom will be attached by caller so we can inject AWS/Vault secrets when needed
         volume_mounts=[
             client.V1VolumeMount(name="workdir", mount_path="/work"),
             client.V1VolumeMount(name="models", mount_path=mount_path),
         ],
     )
@@ -334,28 +209,13 @@ def deploy_vllm(
     init_containers = []
     if artifact_sha256 and signature is not None:
         ic = _artifact_init_container(
             name="aegis-artifact-init",
             artifact_uri=model_artifact_uri,
             artifact_sha256=artifact_sha256,
             signature=signature,
             signature_type=signature_type,
             mount_path="/models",
         )
-        s3_env, s3_env_from = _storage_env_and_secrets(namespace)
-        v_env, v_env_from = _vault_env_and_secrets(namespace)
-        ic.env = (ic.env or []) + s3_env + v_env
-        ic.env_from = (s3_env_from + v_env_from) or None
-        init_containers.append(
-            _artifact_init_container(
-                name="aegis-artifact-init",
-                artifact_uri=model_artifact_uri,
-                artifact_sha256=artifact_sha256,
-                signature=signature,
-                signature_type=signature_type,
-                mount_path="/models",
-            )
-        )
+        init_containers.append(ic)
 
     env = [
         client.V1EnvVar(name="AEGIS_MODEL_ARTIFACT_URI", value=model_artifact_uri),
         client.V1EnvVar(name="AEGIS_MODELS_DIR", value="/models"),
@@ -401,13 +261,13 @@ def deploy_triton(
     init_containers = []
     if artifact_sha256 and signature is not None:
         ic = _artifact_init_container(
             name="aegis-artifact-init",
             artifact_uri=model_artifact_uri,
             artifact_sha256=artifact_sha256,
             signature=signature,
             signature_type=signature_type,
             mount_path="/models",
         )
-        s3_env, s3_env_from = _storage_env_and_secrets(namespace)
-        v_env, v_env_from = _vault_env_and_secrets(namespace)
-        ic.env = (ic.env or []) + s3_env + v_env
-        ic.env_from = (s3_env_from + v_env_from) or None
         init_containers.append(ic)
diff --git a/api/golden_signing.py b/api/golden_signing.py
index 1778c7e..9c2b941 100644
--- a/api/golden_signing.py
+++ b/api/golden_signing.py
@@ -12,13 +12,14 @@ import hmac
 import hashlib
 import base64
 import json
 import requests
 from typing import Optional
 
 
 SIGN_MODE = os.environ.get("AEGIS_SIGN_MODE", "hmac")  # noop|hmac|vault_transit
 HMAC_SECRET = os.environ.get("AEGIS_SIGN_HMAC_SECRET", "dev-sign-secret-change-me")
 
 # Vault Transit config (SIGN_MODE=vault_transit)
 VAULT_ADDR = os.environ.get("VAULT_ADDR", "")
-# Prefer AEGIS_VAULT_TOKEN, fallback to VAULT_TOKEN
-VAULT_TOKEN = os.environ.get("AEGIS_VAULT_TOKEN", "") or os.environ.get("VAULT_TOKEN", "")
+# Hardened: do not require a token in env; prefer Vault Agent injector file if present
+VAULT_TOKEN = os.environ.get("VAULT_TOKEN", "")  # optional fallback
 VAULT_TRANSIT_MOUNT = os.environ.get("AEGIS_VAULT_TRANSIT_MOUNT", "transit")
 VAULT_TRANSIT_KEY = os.environ.get("AEGIS_VAULT_TRANSIT_KEY", "aegis-model-sign")
+VAULT_TOKEN_FILE = os.environ.get("VAULT_TOKEN_FILE", "/vault/secrets/token")
@@ -34,10 +35,16 @@ def sign_sha256(hex_sha256: str) -> str:
     if SIGN_MODE == "hmac":
         return hmac.new(HMAC_SECRET.encode("utf-8"), hex_sha256.encode("utf-8"), hashlib.sha256).hexdigest()
     if SIGN_MODE == "vault_transit":
-        if not (VAULT_ADDR and VAULT_TOKEN):
-            raise RuntimeError("VAULT_ADDR and VAULT_TOKEN required for SIGN_MODE=vault_transit")
+        if not VAULT_ADDR:
+            raise RuntimeError("VAULT_ADDR required for SIGN_MODE=vault_transit")
+        token = VAULT_TOKEN
+        if not token:
+            try:
+                token = open(VAULT_TOKEN_FILE, "r").read().strip()
+            except Exception:
+                token = ""
+        if not token:
+            raise RuntimeError("Vault token missing. Use Vault Agent injector to provide /vault/secrets/token (or set VAULT_TOKEN).")
         # Transit sign expects base64 input
         payload_b64 = base64.b64encode(hex_sha256.encode("utf-8")).decode("utf-8")
         url = f"{VAULT_ADDR}/v1/{VAULT_TRANSIT_MOUNT}/sign/{VAULT_TRANSIT_KEY}"
-        r = requests.post(url, headers={"X-Vault-Token": VAULT_TOKEN}, json={"input": payload_b64}, timeout=10)
+        r = requests.post(url, headers={"X-Vault-Token": token}, json={"input": payload_b64}, timeout=10)
         r.raise_for_status()
         sig = r.json()["data"]["signature"]
         return sig
     raise RuntimeError(f"Unknown SIGN_MODE={SIGN_MODE}")
@@ -50,10 +57,16 @@ def verify_sha256(hex_sha256: str, signature: Optional[str]) -> bool:
     if SIGN_MODE == "vault_transit":
-        if not (VAULT_ADDR and VAULT_TOKEN):
-            raise RuntimeError("VAULT_ADDR and VAULT_TOKEN required for SIGN_MODE=vault_transit")
+        if not VAULT_ADDR:
+            raise RuntimeError("VAULT_ADDR required for SIGN_MODE=vault_transit")
+        token = VAULT_TOKEN
+        if not token:
+            try:
+                token = open(VAULT_TOKEN_FILE, "r").read().strip()
+            except Exception:
+                token = ""
+        if not token:
+            raise RuntimeError("Vault token missing. Use Vault Agent injector to provide /vault/secrets/token (or set VAULT_TOKEN).")
         payload_b64 = base64.b64encode(hex_sha256.encode("utf-8")).decode("utf-8")
         url = f"{VAULT_ADDR}/v1/{VAULT_TRANSIT_MOUNT}/verify/{VAULT_TRANSIT_KEY}"
-        r = requests.post(url, headers={"X-Vault-Token": VAULT_TOKEN}, json={"input": payload_b64, "signature": signature}, timeout=10)
+        r = requests.post(url, headers={"X-Vault-Token": token}, json={"input": payload_b64, "signature": signature}, timeout=10)
         r.raise_for_status()
         return bool(r.json()["data"]["valid"])
     raise RuntimeError(f"Unknown SIGN_MODE={SIGN_MODE}")
diff --git a/api/golden_app.py b/api/golden_app.py
index 6a50f1a..a6c7870 100644
--- a/api/golden_app.py
+++ b/api/golden_app.py
@@ -34,6 +34,7 @@ from prometheus_client import generate_latest, CONTENT_TYPE_LATEST, Counter, Summary
 
 from api.logging_config import configure_logging, set_request_id
 from api.otel import init_otel
 from api.db import engine as sqlalchemy_engine, SessionLocal
 
@@ -64,8 +65,10 @@ CHARS_PER_UNIT = int(os.environ.get("AEGIS_CHARS_PER_UNIT", "400"))  # crude heuristic if tokens not provided
 
 # Proxy hardening
 ALLOW_PROXY = os.environ.get("AEGIS_INFER_PROXY_ENABLED", "false").lower() == "true"
 PROXY_TIMEOUT_S = float(os.environ.get("AEGIS_INFER_PROXY_TIMEOUT_S", "10"))
 # Optional: block proxy to unknown paths (defense in depth)
 VLLM_ROUTE = os.environ.get("AEGIS_VLLM_ROUTE", "/v1/chat/completions")
 
+DISABLE_DOCS = os.environ.get("AEGIS_DISABLE_DOCS", "true").lower() == "true"
 
 configure_logging()
 logger = logging.getLogger("aegis.golden")
 logger.setLevel(logging.INFO)
 
-app = FastAPI(title="Aegis Golden Path API", version="0.1.0")
+app = FastAPI(
+    title="Aegis Golden Path API",
+    version="0.2.0",
+    docs_url=None if DISABLE_DOCS else "/docs",
+    redoc_url=None if DISABLE_DOCS else "/redoc",
+    openapi_url=None if DISABLE_DOCS else "/openapi.json",
+)
diff --git a/api/requirements-golden.txt b/api/requirements-golden.txt
index 6f08b70..f45c6dc 100644
--- a/api/requirements-golden.txt
+++ b/api/requirements-golden.txt
@@ -1,9 +1,14 @@
 # Golden-path dependencies
 -r api/requirements-api2.txt
 -r api/requirements-db.txt
 
 prometheus-client==0.20.0
 httpx==0.27.0
 boto3==1.34.131
 kubernetes==29.0.0
 requests==2.32.3
+
+# Ensure FastAPI stack can run with hardened defaults (already in requirements-api2.txt)
+# python-json-logger is used by api/logging_config.py
diff --git a/README_GOLDEN_PATH.md b/README_GOLDEN_PATH.md
index 3e9a62c..f2d7a5d 100644
--- a/README_GOLDEN_PATH.md
+++ b/README_GOLDEN_PATH.md
@@ -1,6 +1,6 @@
 # Aegis Golden Path (v1)
 
 Implements the minimal end-to-end platform loop:
@@ -22,6 +22,7 @@ python -m api.bootstrap_golden_db
 
 ## Required env vars
 
 ### Signing (upgrade: Vault Transit)
 You can keep `hmac` for dev, but for production use Vault Transit:
@@ -29,12 +30,20 @@ You can keep `hmac` for dev, but for production use Vault Transit:
 - `AEGIS_SIGN_MODE=vault_transit`
 - `VAULT_ADDR=https://vault.example.internal:8200`
-- `VAULT_TOKEN=...` (short-lived token is strongly recommended)
 - `AEGIS_VAULT_TRANSIT_MOUNT=transit`
 - `AEGIS_VAULT_TRANSIT_KEY=aegis-model-sign`
 
-If you run in Kubernetes, prefer Vault Agent/Kubernetes auth rather than injecting VAULT_TOKEN directly.
+Hardened (recommended): **do not inject VAULT_TOKEN**.
+Use Vault Agent injector (Kubernetes auth) to provide a token file:
+- `/vault/secrets/token` (default)
+or set:
+- `VAULT_TOKEN_FILE=/vault/secrets/token`
 
 ### Auth (OIDC JWKS)
 - `OIDC_JWKS_URI` (required)
 - `OIDC_AUDIENCE` (recommended)
 - `OIDC_ISSUER` (recommended)
 - `AEGIS_TENANT_CLAIM` (default: `tenant`)
 - `AEGIS_ROLES_CLAIM` (default: `roles`)
@@ -67,31 +76,20 @@ MinIO in-cluster:
 ### Runtime artifact fetch/verify (initContainer)
 Deployments now include an initContainer that:
 1) downloads the artifact from S3/MinIO
 2) verifies sha256 and signature
 3) extracts/copies into `/models` (emptyDir shared with Triton/vLLM)
 
-For AWS S3 on EKS:
-- Prefer IRSA so pods do not need static credentials.
-
-For MinIO:
-- Set these env vars on the Aegis API (temporary/self-contained option):
-  - `AEGIS_MINIO_ACCESS_KEY=...`
-  - `AEGIS_MINIO_SECRET_KEY=...`
-  The deployer will create a tenant-namespace secret `aegis-s3-creds` and attach it to initContainers.
-
-Production recommendation:
-- Replace this with External Secrets Operator or Vault Agent injector.
-
-### Vault auth (self-contained vs production)
-Self-contained (NOT recommended long term):
-- Set `AEGIS_VAULT_TOKEN=...` on the Aegis API
-  - deployer will create secret `aegis-vault-token` in each tenant namespace
-
-Production recommendation:
-- Use Vault Kubernetes auth + Vault Agent injector to provide a short-lived token to the initContainer,
-  or use External Secrets to sync Vault token/approles into namespace.
+Hardened storage guidance:
+- AWS S3 on EKS: use **IRSA** (no static creds in pods).
+- MinIO: provide creds via **External Secrets** (or another secret manager) into the tenant namespace.
+  This hardened patch intentionally **does not** copy credentials into tenant namespaces.
+
+Hardened Vault guidance:
+- Use **Vault Agent injector** to provide token file to initContainers.
 
 ## Kubernetes config
 - `AEGIS_K8S_CONFIG=auto|incluster|kubeconfig`
+
+## API hardening knobs
+- `AEGIS_DISABLE_DOCS=true` (default) disables OpenAPI docs endpoints.
